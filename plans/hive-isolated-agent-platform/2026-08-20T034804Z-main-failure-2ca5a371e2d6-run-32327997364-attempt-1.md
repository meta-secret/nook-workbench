---
title: Restore Main Web e2e after the Access browse-row removal
feature: hive-isolated-agent-platform
issue: issues/hive-isolated-agent-platform/main-failure-2ca5a371e2d616a8beaa8360cb4712d6c6279299.md
started_at: 2026-08-20T03:44:35Z
agent: codex
---

# Restore Main Web e2e after the Access browse-row removal

## Interpreted request

Diagnose the failed Main browser suite at the retained revision, repair the
root cause through a normal exact-head pull request, and keep ownership through
green Main verification and durable incident records.

## Requirements

- Preserve the failed Main run and revision as evidence.
- Use the retained Playwright output and application-log trail to identify the
  failing behavior.
- Make the smallest correction with a behavior-focused regression assertion.
- Validate the exact pull-request head with the Main-equivalent browser suites.
- Mark the pull request with `hive` and `ci:full-e2e`.
- Resolve actionable review feedback, squash-merge, and verify the resulting
  Main workflow.
- Publish the incident completion update, linked worklog, and agent statistics.

## Constraints and exclusions

- Do not push directly to Main, bypass checks, or weaken cache isolation.
- Do not reproduce raw logs, credentials, or private data in durable records.
- Keep product behavior unchanged when the failure is an obsolete test
  expectation rather than an application defect.
- Estimated authored change is under 20 lines and belongs to the Access web e2e
  flow.
- No delegation is needed for this single-locator regression.

## Change budget and PR sequence

- Estimated authored changed lines: 8
- Owning modules, packages, or layers: Access Playwright coverage in nook-web-app
- Public or cross-module interfaces: No interface changes; test expectations only
- Delivery shape: One PR
- Current PR estimated authored changed lines: 8
- Current PR slice and acceptance evidence: Replace the obsolete Access browse-row expectation; Acceptance evidence: exact-head full e2e and repository checks pass
- PR slices and acceptance evidence:
Replace the obsolete Access browse-row expectation; Acceptance evidence: exact-head full e2e and repository checks pass

## Initial plan

1. Correlate the failed locator, persisted application milestones, and the
   changes immediately preceding the failed Main revision.
2. Replace the obsolete browse-row assertion with checks for the intended
   selected Identity tab and one-vault headline.
3. Run repository pre-push hygiene, create a focused commit and pull request,
   and attach the required labels.
4. Run exact-head repository validation with full e2e, address feedback, audit
   readiness, and squash-merge.
5. Verify the resulting Main workflow and publish Workbench completion records
   and statistics.

## Completion evidence

- A merged Nook pull request whose exact head passes repository-owned checks,
  including Main-equivalent browser suites.
- A successful Main workflow for the squash commit.
- Updated Workbench incident, linked completion worklog, and agent statistics.

## Safety review

- This plan contains no raw prompt, chat transcript, secrets, private data, raw
  logs, local paths, or unnecessary infrastructure details.
