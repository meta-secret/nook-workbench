---
title: Restore Main WASM cache verification on ARC
feature: hive-isolated-agent-platform
issue: issues/hive-isolated-agent-platform/main-failure-46568599ac5b5e0435bc494f46501793adac3cb5.md
started_at: 2026-08-23T01:05:37Z
agent: codex
---

# Task plan

## Interpreted request

Restore the failed Main revision through a normal reviewed repair PR. Diagnose the retained WASM-verification evidence, fix the runner-incompatible cache proof without weakening its registry-reuse or isolation guarantees, prove the behavior with focused regression coverage, run Main-equivalent PR checks, merge the exact reviewed head, and record the successful resulting Main run.

## Requirements

- Preserve the failed revision and workflow link as incident evidence.
- Keep the proof that required WASM dependency layers restore from the published registry cache.
- Support the isolated ARC job BuildKit without starting a nested container runtime.
- Retain the hosted runner's fresh container-builder proof.
- Add behavior-focused regression coverage for both runner paths.
- Apply both required delivery labels: `ci:full-e2e` plus `hive`.
- Pass exact-head repository-owned validation and review, squash-merge, verify Main, and publish completion records and statistics.

## Constraints and exclusions

- Do not alter product behavior, bypass checks, relax cache isolation, push to Main, or expose raw logs or credentials.
- Heavy product validation runs only through GitHub Actions.
- The repair is one small engineering-harness PR with no public API change.

## Change budget and PR sequence

- Estimated authored changed lines: 150
- Owning modules, packages, or layers: engineering harness; WASM cache verifier; preflight harness contracts
- Public or cross-module interfaces: no public interface; internal CI script behavior only
- Delivery shape: One PR
- Current PR estimated authored changed lines: 150
- Current PR slice and acceptance evidence: select a fresh cache-consumer proof compatible with hosted and ARC runners; Acceptance evidence: focused preflight contracts and full PR/Main-equivalent checks
- PR slices and acceptance evidence: select a fresh cache-consumer proof compatible with hosted and ARC runners; Acceptance evidence: focused preflight contracts and full PR/Main-equivalent checks

## Initial plan

1. Make the WASM cache verifier reuse the isolated ARC job builder only after clearing its local build records, while hosted runners keep a newly created container builder.
2. Extend preflight contracts to require both paths and the registry-only restore markers.
3. Format, review, publish, and run full exact-head PR validation.
4. Address all actionable review, audit readiness, squash-merge, verify Main, and publish Workbench records.

## Completion evidence

- Focused preflight regression coverage passes in repository-owned CI.
- Exact-head PR validation, including both full browser suites, is green.
- Readiness audit passes with no unresolved actionable feedback.
- Squash merge completes and the resulting Main workflow succeeds.
- The incident, worklog, and AI-agent statistics link the delivered PR and validation.

## Safety review

- This record contains no raw prompt, chat transcript, secrets, private data, raw logs, local paths, or unnecessary infrastructure detail.
