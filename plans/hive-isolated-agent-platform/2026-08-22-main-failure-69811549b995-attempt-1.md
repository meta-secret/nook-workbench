# Task plan

## Interpreted request

Repair the Main browser verification failure introduced with the application-isolation coverage, preserve the failed-run evidence in Workbench, and deliver the focused fix through a reviewed squash-merged pull request.

## Requirements

- Explain why the Simple application-isolation browser flow timed out.
- Initialize the extension-side device through the supported UI before reading its pairing identity.
- Keep the Simple extension-pairing and Sentinel isolation assertions intact.
- Validate the precise pull-request revision with repository checks and browser coverage matching default-branch verification.
- Label the pull request for Hive ownership and full end-to-end validation.
- Record the merged pull request, validation, Main result, worklog, and agent statistics in Workbench.

## Constraints and exclusions

- Do not change production authentication, authorization, device-identity, cryptographic, storage, or cache boundaries.
- Do not bypass checks, increase timeouts to hide the race, weaken assertions, or update the default branch outside a pull request.
- Keep domain behavior in Rust and use TypeScript only for the browser test flow.
- Keep the broken commit identifier and Actions link as incident evidence without publishing retained job output.

## Change budget and PR sequence

- Estimated authored changed lines: 35
- Owning modules, packages, or layers: nook-web-app Playwright application-isolation coverage and Workbench delivery records
- Public or cross-module interfaces: None
- Delivery shape: One PR
- Current PR estimated authored changed lines: 35
- Current PR slice and acceptance evidence: Initialize the isolated extension browser device before exercising Simple pairing; Acceptance evidence: the application-isolation regression and Main-equivalent browser suites pass on the exact pull-request head
- PR slices and acceptance evidence:
  Initialize the isolated extension browser device before exercising Simple pairing; Acceptance evidence: the application-isolation regression and Main-equivalent browser suites pass on the exact pull-request head

## Initial plan

1. Branch from the latest Main revision and preserve the failed workflow reference.
2. Replace the unsupported fresh-manager identity read with the existing UI vault-creation helper and assert the initialized session before collecting pairing keys.
3. Format, review, publish, and run exact-head full browser validation.
4. Address checks and review, run readiness, squash-merge, verify Main, and publish completion records.

## Completion evidence

Completion requires a squash-merged Hive pull request, green exact-head repository checks including full browser validation, a green resulting Main workflow, and immutable Workbench statistics plus an updated incident worklog.

## Safety review

This plan contains no copied request text, conversation transcript, access material, private user or vault data, runtime configuration value, retained job output, workstation path, or internal host detail. It changes test setup only and does not weaken security or storage boundaries.
