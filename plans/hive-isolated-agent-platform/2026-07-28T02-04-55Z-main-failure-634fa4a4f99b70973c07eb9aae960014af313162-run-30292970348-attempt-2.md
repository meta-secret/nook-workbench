---
title: Repair failed Main verification for 634fa4a4f99b
feature: hive-isolated-agent-platform
issue: issues/hive-isolated-agent-platform/main-failure-634fa4a4f99b70973c07eb9aae960014af313162.md
started_at: 2026-07-28T02:04:55Z
agent: codex
---

# Repair failed Main verification for 634fa4a4f99b

## Interpreted request

Identify and repair the browser-suite regression recorded against the failed
Main revision, validate a scoped pull request with the Main-equivalent suites,
then squash-merge and verify the resulting Main run. Preserve the failed-run
evidence by linking it rather than copying logs.

## Requirements

- Diagnose the retained workflow artifacts and implement only the root-cause
  correction with browser regression coverage.
- Use a normal repair branch and pull request, apply `ci:full-e2e`, resolve
  actionable feedback, run the guest readiness audit, and squash-merge only
  after exact-head repository checks pass.
- Record the completed pull request, validation, and statistics in Workbench.

## Constraints and exclusions

- Do not push directly to `main`, bypass checks, weaken cache boundaries, or
  place raw logs or sensitive values in Workbench records.
- First resume an existing publication branch when it owns the same root cause;
  do not create a competing repair.

## Initial plan

1. Inspect the failed workflow, retained artifacts, existing branches, pull
   requests, and later Main state.
2. Resume the matching repair when one is active, or create the deterministic
   repair branch from current `main` and add the minimal fix plus browser test.
3. Publish, validate, resolve feedback, audit, squash-merge, and verify Main.
4. Publish the incident completion record, linked worklog, and PR statistics.

## Completion evidence

- A linked repair PR with `ci:full-e2e`, green exact-head repository checks,
  a successful squash merge, and a green resulting Main workflow.
- Workbench incident, worklog, and statistics records linked to the repair.

## Safety review

This plan contains an interpreted scope only. It excludes prompts, transcripts,
credentials, raw logs, local paths, private data, and unnecessary infrastructure
details.
