---
title: Restore Main verification for 401cd489392e
feature: hive-isolated-agent-platform
issue: issues/hive-isolated-agent-platform/main-failure-401cd489392e65e63718058677301ad36f358fac.md
started_at: 2026-07-28T11:15:00Z
agent: codex
---

# Task plan

## Interpreted request

Restore trusted Main verification after the browser suites failed on the
revision produced by PR 846. Deliver any necessary repair through a normal
Nook pull request and close the durable Hive incident only after the resulting
Main verification succeeds.

## Requirements

- Diagnose the retained Web and Extension browser-suite evidence without
  publishing raw logs or sensitive data.
- Make the smallest behavior-preserving fix with browser regression coverage.
- Require exact-head repository checks, including the Main-equivalent browser
  suites, by applying `ci:full-e2e` to the repair PR.
- Squash-merge only after readiness, review, and feedback checks are complete,
  then verify the replacement Main workflow and record delivery in Workbench.

## Constraints and exclusions

- Preserve the failed revision and linked workflow evidence; do not push to
  Main, bypass checks, weaken cache isolation, or alter security boundaries.
- First determine whether a later, already-merged change and its active Main
  run have resolved the failure, to avoid a duplicate repair.
- Workbench records contain summaries and links only, never raw logs,
  credentials, private data, or local paths.

## Initial plan

1. Inspect the incident, failed workflow evidence, current Main, and any
   existing repair state.
2. If still needed, isolate the common browser failure, add focused regression
   coverage, and publish the deterministic repair branch and PR.
3. Apply the required label, follow exact-head checks and feedback, and run
   the Hive readiness audit before squash merging.
4. Verify the replacement Main run and publish the incident outcome, worklog,
   and PR statistics to Workbench.

## Completion evidence

- Linked repair PR, its green exact-head checks and `ci:full-e2e` label.
- Green replacement Main workflow linked from the incident.
- Updated incident plus linked worklog and immutable PR statistics record.

## Safety review

This plan is an operational summary and contains no raw prompt, transcript,
secrets, private data, raw logs, local paths, or unnecessary infrastructure
detail.
