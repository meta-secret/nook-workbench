---
title: Restore Main WASM cache verification on ARC
feature: hive-isolated-agent-platform
issue: issues/hive-isolated-agent-platform/main-failure-01f3cb3c6d313a76307b3ba0cb120875e305a821.md
started_at: 2026-08-24T02:59:32Z
agent: codex
---

# Task plan

## Interpreted request

Restore the failed Main integration through a reviewed repair PR. Fix the ARC-incompatible WASM cache proof while preserving cold registry-reuse evidence, add focused regression coverage, run the Main-equivalent exact-head gate, squash-merge, verify the resulting Main run, and publish the incident completion records.

## Requirements

- Preserve the failed revision and workflow link as incident evidence.
- Keep the proof that required WASM dependency layers restore from the published registry cache.
- Support the isolated ARC job BuildKit without starting nested BuildKit through Podman.
- Retain the disposable fresh-builder proof on hosted runners.
- Cover both runner paths and unsafe ARC-builder selection.
- Apply the `hive` and `ci:full-e2e` labels.
- Pass exact-head repository checks and review, squash-merge, verify Main, and publish Workbench completion records and statistics.

## Constraints and exclusions

- Do not alter product behavior, bypass checks, relax cache isolation, push to Main, expose raw logs, or mutate the stale older repair PR.
- Heavy product validation runs only through GitHub Actions.
- The repair is one focused engineering-harness PR with no public API change.

## Change budget and PR sequence

- Estimated authored changed lines: 170
- Owning modules, packages, or layers: engineering harness; WASM cache verifier; preflight harness contracts
- Public or cross-module interfaces: no public interface; internal CI environment marker and script behavior only
- Delivery shape: One PR
- Current PR estimated authored changed lines: 170
- Current PR slice and acceptance evidence: cold WASM registry-cache proof compatible with hosted and ARC runners; Acceptance evidence: focused shell behavior tests, preflight contracts, and full PR/Main-equivalent checks
- PR slices and acceptance evidence:
  1. Cold WASM registry-cache proof compatible with hosted and ARC runners; Acceptance evidence: focused shell behavior tests, preflight contracts, and full PR/Main-equivalent checks

## Initial plan

1. Mark the ARC BuildKit selection explicitly and validate that the verifier targets only the exact current run/job/attempt builder.
2. Empty and reuse that private job sidecar for the cold registry proof; retain fresh container builders for hosted runners.
3. Add focused behavior tests and strengthen preflight contracts for both paths.
4. Format, review, publish, validate, address feedback, audit readiness, and squash-merge.
5. Verify the resulting Main workflow and publish the incident, worklog, and agent statistics.

## Completion evidence

- Focused behavior and preflight coverage passes in repository-owned CI.
- Exact-head PR validation, including both Main-equivalent browser suites, is green.
- Readiness audit passes with no unresolved actionable feedback.
- Squash merge completes and the resulting Main workflow succeeds.
- Workbench incident, worklog, and statistics identify the delivered PR and validation.

## Safety review

- This record contains no raw prompt, chat transcript, secrets, private data, raw logs, local paths, or unnecessary infrastructure detail.
