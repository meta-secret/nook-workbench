---
title: Restore ARC WASM cache verification
feature: hive-isolated-agent-platform
issue: issues/hive-isolated-agent-platform/main-failure-46568599ac5b5e0435bc494f46501793adac3cb5.md
started_at: 2026-08-24T03:01:13Z
agent: codex
---

# Restore ARC WASM cache verification

## Interpreted request

Restore trusted Main delivery by correcting the repeatable WASM cache-proof failure introduced when Main moved to isolated ARC runners. Preserve the failed revision and workflow evidence, keep cache isolation strict, and own the normal pull request through exact-head checks, review, squash merge, green Main, and incident completion.

## Requirements

- Explain the retained Main failure using public-safe workflow evidence.
- Preserve the existing verified cache publication and fresh-cache reuse proof.
- Avoid nested BuildKit containers where ARC supplies one private job-scoped remote builder.
- Reject any attempt to prune a builder not owned by the current workflow job.
- Retain disposable fresh-builder behavior on GitHub-hosted runners.
- Add behavior-focused shell coverage and repository preflight contract coverage.
- Apply the `hive` and `ci:full-e2e` labels and pass Main-equivalent browser suites.
- Squash-merge only after exact-head readiness and verify the resulting Main workflow.

## Constraints and exclusions

- Do not weaken cache isolation, bypass checks, push directly to Main, or expose infrastructure credentials.
- Do not change WASM domain behavior; the fault is in CI cache-proof orchestration.
- Heavy verification runs only through GitHub Actions.
- The existing deterministic branch is the owned generation because it has no closed or merged pull request.

## Change budget and PR sequence

- Estimated authored changed lines: 137
- Owning modules, packages, or layers: GitHub Actions Docker setup, WASM registry-cache verifier, and preflight delivery contracts.
- Public or cross-module interfaces: CI environment contract for `NOOK_BUILDKIT_REMOTE` and `NOOK_PR_BUILDX_BUILDER`; no product API changes.
- Delivery shape: One PR
- Current PR estimated authored changed lines: 137
- Current PR slice and acceptance evidence: Repair ARC WASM cache proof builder selection; Acceptance evidence: shell and Rust preflight tests, exact-head checks, Main-equivalent browser suites, and post-merge Main pass.
- PR slices and acceptance evidence: Repair ARC WASM cache proof builder selection; Acceptance evidence: shell and Rust preflight tests, exact-head checks, Main-equivalent browser suites, and post-merge Main pass.

## Initial plan

1. Inspect retained workflow evidence and current Main repetitions.
2. Rebase the existing focused repair onto current Main and verify its regression coverage.
3. Format, review, publish, and open a marked Hive pull request.
4. Run complete full-E2E validation, address all actionable feedback, and audit exact-head readiness.
5. Squash-merge, verify green Main, and publish Workbench completion records and agent statistics.

## Completion evidence

- A `[Hive]` pull request linked to the incident with both required labels.
- Green exact-head repository checks, full browser suites, and no unresolved actionable review.
- Successful guest readiness audit, squash merge, and green Main workflow for the merge revision.
- Updated incident, completion worklog, and published agent statistics.

## Safety review

- This plan contains only public-safe development context and no sensitive or private material.
