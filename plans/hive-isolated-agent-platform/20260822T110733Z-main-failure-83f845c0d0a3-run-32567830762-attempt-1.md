---
title: Restore Main native verification after transient frontend resolution failure
feature: hive-isolated-agent-platform
issue: issues/hive-isolated-agent-platform/main-failure-83f845c0d0a3b535b4b0db46efe918ef34314bef.md
started_at: 2026-08-22T11:07:33Z
agent: codex
---

# Task plan

## Interpreted request

Restore the failed Main revision through a normal reviewed repair PR. Diagnose the retained native-verification evidence, fix the underlying harness weakness without weakening cache or verification boundaries, prove the behavior with a focused regression, run Main-equivalent PR checks, merge the exact reviewed head, and record the successful resulting Main run.

## Requirements

- Preserve the failed revision and workflow link as incident evidence.
- Retry only the observed transient BuildKit frontend-resolution class.
- Continue to fail closed for application failures and genuine S3 health failures.
- Add behavior-focused regression coverage for the transient authorization timeout.
- Deliver on the deterministic Hive branch with `hive` and `ci:full-e2e` labels.
- Pass exact-head repository-owned validation and review, squash-merge, verify Main, and publish completion records and statistics.

## Constraints and exclusions

- Do not alter product Rust behavior, bypass checks, relax cache isolation, push to Main, or expose raw logs or credentials.
- Heavy product validation runs only through GitHub Actions.
- The repair is one small harness PR; no product or public API change is expected.

## Change budget and PR sequence

- Estimated authored changed lines: 30
- Owning modules, packages, or layers: engineering harness; BuildKit retry helper; native platform Taskfile
- Public or cross-module interfaces: no public interface; internal shell helper invocation only
- Delivery shape: One PR
- Current PR estimated authored changed lines: 30
- Current PR slice and acceptance evidence: retry transient frontend token TLS failures in the ARC sccache probe; Acceptance evidence: shell regression and full PR/Main-equivalent checks
- PR slices and acceptance evidence: retry transient frontend token TLS failures in the ARC sccache probe; Acceptance evidence: shell regression and full PR/Main-equivalent checks

## Initial plan

1. Route the ARC sccache probe through the existing fail-closed frontend retry helper.
2. Extend its transient classifier and shell regression test for Docker Hub token TLS timeout evidence.
3. Format, review, publish, and run full exact-head PR validation.
4. Address all actionable review, audit readiness, squash-merge, verify Main, and publish Workbench records.

## Completion evidence

- Focused shell regression passes in repository-owned CI.
- Exact-head PR validation, including both full browser suites, is green.
- Readiness audit passes with no unresolved actionable feedback.
- Squash merge completes and the resulting Main workflow succeeds.
- The incident, worklog, and AI-agent statistics link the delivered PR and validation.

## Safety review

- This record contains no raw prompt, chat transcript, secrets, private data, raw logs, local paths, or unnecessary infrastructure detail.
