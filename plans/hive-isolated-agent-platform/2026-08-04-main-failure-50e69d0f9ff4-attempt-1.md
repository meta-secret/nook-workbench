---
title: Main repair plan for failed Web e2e verification
feature: hive-isolated-agent-platform
incident: main-failure-50e69d0f9ff4d97f14e6590a0f291318ba1adebf
---

# Main repair plan: failed Web e2e verification

## Interpretation

Repair the responsive Devices & Access identity bridge after Main run 30935881833
failed its browser suite. Preserve the existing Main evidence, make the smallest
presentation-layer correction, and prove the compact bridge keeps its protection
node within the graph at the failed viewport.

## Constraints

- Work through a normal Nook pull request; never push directly to Main.
- Preserve identity, vault, and device-key policy in the Rust/WASM boundary.
- Run the Main-equivalent exact-head validation and resolve current feedback.

## Plan

1. Inspect the retained Web e2e failure and app-log attachment, then trace the
   compact bridge geometry.
2. Correct the responsive graph/node sizing and extend the focused Playwright
   regression around the failed viewport.
3. Format, publish a labeled Hive PR, run exact-head full-e2e validation,
   readiness audit, squash merge, and verify the resulting Main run.

## Completion evidence

Record the merged PR, exact-head validation, and green Main workflow in the
linked incident worklog.
