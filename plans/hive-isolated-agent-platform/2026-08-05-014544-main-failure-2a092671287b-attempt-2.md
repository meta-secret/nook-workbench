---
title: Repair plan for persistent-route verification retry
feature: hive-isolated-agent-platform
issue: issues/hive-isolated-agent-platform/main-failure-2a092671287bca3c174b6f30d758c27d73047013.md
started_at: 2026-08-05T01:45:44Z
agent: codex
---

# Repair plan

## Interpreted request

Complete the existing repair PR for the failed persistent-route Main run by
using its retained browser evidence, correcting the unlock regression, and
delivering the validated merge and incident records.

## Requirements

- Retrieve and use the failed browser job evidence and persisted application
  logs.
- Keep the existing PR branch, retain secure vault-unlock behavior, and add
  focused regression coverage for the observed flow.
- Format, validate the replacement PR head with the Main-equivalent browser
  suite, squash-merge, verify Main, and publish completion records.

## Constraints and exclusions

- Do not create a duplicate repair PR or weaken authentication, encryption,
  route, or cache boundaries.
- Do not publish raw logs, credentials, vault data, or chat content.
- Limit changes to the existing repair's failure and its required evidence.

## Initial plan

1. Inspect the live repair PR and retrieve the retained failed-job evidence.
2. Trace the successful unlock state through workspace route rendering.
3. Implement the narrow repair and regression coverage on the existing branch.
4. Format, push, run exact-head full validation, resolve review feedback, and
   squash-merge.
5. Verify the delivered Main run and update the incident, worklog, and agent
   statistics on Workbench.

## Completion evidence

- A merged PR linked to a successful exact-head full browser validation.
- A successful Main run containing the merge and published incident completion
  records.

## Safety review

This plan contains only a public-safe summary. It excludes raw prompts, chat
transcripts, secrets, private data, raw logs, and local environment details.
