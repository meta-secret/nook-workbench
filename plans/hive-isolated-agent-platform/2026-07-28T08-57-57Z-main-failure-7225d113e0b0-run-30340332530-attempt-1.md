---
title: Repair Main browser verification after provider-store split
feature: hive-isolated-agent-platform
issue: issues/hive-isolated-agent-platform/main-failure-7225d113e0b0de879f8ea77b9a318471e0781703.md
started_at: 2026-07-28T08:57:57Z
agent: codex
---

# Task plan

## Interpreted request

Restore the default branch's browser verification after the provider-store
refactor introduced a failure across web, extension, and UI-demo suites. Deliver
the smallest safe correction as a reviewed pull request, then verify its
squash-merged Main revision.

## Requirements

- Diagnose the retained failed run and preserve only its public-safe summary.
- Add behavior-focused browser regression coverage for the failed lifecycle.
- Apply the `ci:full-e2e` PR label and use repository-owned exact-head checks.
- Resolve actionable review feedback, readiness audit findings, and merge by
  squash only.
- Record the result, validation, and agent statistics in Workbench.

## Constraints and exclusions

- Do not change Main directly, bypass checks, or weaken cache or vault
  security boundaries.
- Keep failure records free of raw logs, credentials, and private data.
- Limit product changes to the causal browser lifecycle fault and its
  regression coverage.

## Initial plan

1. Inspect the failed browser jobs and trace the regression to the provider
   store and vault lifecycle implementation.
2. Create a repair branch from current Main, implement the minimal fix, and add
   a targeted browser test.
3. Format, publish a labeled PR, and address repository checks and feedback on
   the exact head.
4. Run the Hive readiness audit, squash-merge, verify the resulting Main run,
   and publish the incident worklog and statistics.

## Completion evidence

- A linked repair PR has green repository-owned validation and is squash-merged.
- The post-merge Main browser verification is green.
- The Workbench incident, worklog, and statistics link the PR and validation.

## Safety review

This plan contains an implementation summary and public workflow references
only; it contains no prompt transcript, raw logs, credentials, private data,
local paths, or unnecessary infrastructure details.
