---
title: Repair Main browser verification after WASM adapter split
feature: hive-isolated-agent-platform
issue: issues/hive-isolated-agent-platform/main-failure-402e59c8be52d500312112c0ea39b28f91a865f2.md
started_at: 2026-07-28T06:25:00Z
agent: codex
---

# Task plan

## Interpreted request

Diagnose the failed browser checks on the current Main revision, implement the
smallest safe repair with regression coverage, and land it through a normal
squash-merged pull request. Record the incident outcome and validation in
Workbench after the repaired Main workflow succeeds.

## Requirements

- Preserve the failed revision and workflow reference while using retained
  evidence to identify the shared browser failure.
- Keep cryptographic, authentication, device-identity, storage, and cache
  isolation guarantees intact.
- Exercise the repaired behavior with targeted browser coverage and the
  repository-owned exact-head checks, including Main-equivalent browser suites.
- Apply and verify the `ci:full-e2e` PR label, resolve existing actionable
  feedback, and squash-merge only after readiness succeeds.

## Constraints and exclusions

- Work only through a branch and pull request; do not alter Main directly or
  bypass checks.
- Do not store raw logs, credentials, private data, or local paths in Workbench.
- Scope is limited to the root cause of this Main failure; unrelated cleanup is
  excluded.

## Initial plan

1. Inspect the failed jobs and retained artifacts to identify the earliest
   shared fault.
2. Branch from the current Main head, implement the smallest behavior-preserving
   fix, and add focused regression coverage.
3. Format, push, open or update the deterministic repair PR, and apply the
   required browser-suite label.
4. Monitor exact-head checks and feedback, fix any findings, complete readiness,
   squash-merge, then verify the resulting Main run and publish durable records.

## Completion evidence

- A linked repair PR with green applicable repository-owned checks and a
  successful readiness audit.
- A successful post-merge Main workflow and Workbench issue, worklog, and
  statistics records linking the repair and validation.

## Safety review

This record contains a synthesized task description only. It excludes the raw
request, raw logs, credentials, private data, local paths, and unnecessary
infrastructure details.
