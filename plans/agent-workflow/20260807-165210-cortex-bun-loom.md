---
title: Cortex Bun Loom mechanical agent CLIs
feature: agent-workflow
issue: issues/unplanned/README.md
started_at: 2026-08-07T16:51:00Z
agent: cursor
---

# Task plan

## Interpreted request

Replace mechanical `.cortex` agent runbooks with a Bun CLI project
`agentic-ai/loom` so agents run deterministic scripts instead of copying bash
rituals. Keep architecture, product, and judgment prose in cortex.

## Requirements

- Ship `agentic-ai/loom` with Bun, strict TypeScript, tests, and Task targets.
- Implement `pre-push`, `agent-stats`, `cortex-audit`, `skill-scaffold`, and `pr-land`.
- Thin the hottest cortex docs to invoke Loom or existing Task/preflight gates.
- Do not reimplement Rust preflight gates in Bun.
- Deliver through a current, exact-head validated, squash-merged pull request.

## Constraints and exclusions

- Design-docs and product narrative stay prose.
- `ci-agent` stays npm/Node; Loom is Bun-only.
- Merge remains agent-gated; Loom does not squash-merge by default.
- Slip39 leak lint and other deferred Phase 3 items are out of scope.

## Initial plan

1. Scaffold Loom package and Task wiring.
2. Implement Phase 1 commands with focused unit tests.
3. Add Phase 2 `pr-land` assist.
4. Rewrite cortex contracts and AGENTS.md discovery section.
5. Format, push, hosted validate, address feedback, squash merge.
