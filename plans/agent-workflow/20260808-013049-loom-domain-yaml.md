---
title: Loom domain-specific YAML requests
feature: agent-workflow
issue: issues/unplanned/README.md
started_at: 2026-08-08T01:30:00Z
agent: cursor
---

# Task plan

## Interpreted request

Replace Loom's generic name/arguments envelope with domain-tagged YAML
request variants. Keep Loom under the same TypeScript explicit-state rules as
nook-web (enums, Maybe, no authored null/undefined).

## Requirements

- One root domain key per request (prePush, agentStatsAssemble, …)
- No generic arguments field
- Tagged union decode with deny-unknown-fields
- Update params examples, cortex loom-tools docs, tests
- Preserve loom:run CONFIG= Task surface

## Out of scope

- MCP stdio server
