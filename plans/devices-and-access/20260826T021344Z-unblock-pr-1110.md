---
title: Unblock and merge the protected identity switch repair
feature: devices-and-access
issue: issues/devices-and-access/independent-local-identity-keyring.md
started_at: 2026-08-26T02:13:44Z
agent: codex
---

# Unblock and merge the protected identity switch repair

## Interpreted request

Take explicit ownership of the two full-browser failures that block PR 1110, repair their root causes without weakening validation, and complete the existing protected identity switch delivery through readiness, squash merge, and Workbench publication.

This plan supersedes `plans/devices-and-access/20260826T001531Z-fix-protected-identity-switch.md` only for the newly authorized blocker scope.

## Requirements

- Preserve the completed zero-vault **Use identity** fix and its browser evidence.
- Make destructive local browser-copy deletion observably complete before its user flow reports success, including IndexedDB, browser storage, cache, cookie, and open-tab invalidation contracts.
- Make Sentinel invitation response generation survive the local device-protection lifecycle and return the signed, session-bound public response after setup.
- Preserve Rust/WASM authority for device identity, Sentinel validation, and protected material.
- Keep browser code limited to lifecycle coordination and typed transport.
- Retain the full-browser gate; do not skip, quarantine, or soften either failing scenario.
- Run host pre-push formatting before every push, focused hosted browser proof when useful, and full exact-head validation before readiness.
- Resolve current-head review feedback, squash-merge PR 1110, and publish the completion worklog and agent statistics.

## Constraints and exclusions

- No changes to Sentinel cryptographic payloads, quorum rules, or persisted vault formats.
- No weakening of local deletion guarantees or test assertions.
- No local heavy builds or browser suites.
- No unrelated refactor of storage or onboarding surfaces.
- The current PR remains the single delivery vehicle because the user explicitly authorized these blockers as prerequisites to merging it.

## Change budget and PR sequence

- Estimated authored changed lines: 260
- Owning modules, packages, or layers: shared vault web lifecycle, Sentinel genesis join presentation lifecycle, and Playwright regression coverage.
- Public or cross-module interfaces: none expected; existing Rust/WASM contracts remain unchanged.
- Delivery shape: One PR
- Current PR estimated authored changed lines: 260
- Current PR slice and acceptance evidence: Repair the identity switch and both browser blockers in PR 1110; Acceptance evidence: both blocker scenarios pass without retries, the zero-vault identity switch and UI demo remain green, and exact-head readiness succeeds.
- PR slices and acceptance evidence: Repair the identity switch and both browser blockers in PR 1110; Acceptance evidence: both blocker scenarios pass without retries, the zero-vault identity switch and UI demo remain green, and exact-head readiness succeeds.

## Initial plan

1. Trace destructive cleanup completion and Sentinel join intent across the existing web lifecycle, using persisted app logs and failure evidence already captured from exact-head CI.
2. Correct the narrow lifecycle defects and add or refine browser assertions that prove deterministic completion.
3. Run `task loom:pre-push`, commit, run advisory review, and push the replacement head.
4. Run focused hosted browser proof when it shortens diagnosis, then trigger full exact-head validation and current-head cloud review.
5. Address findings until `task pr:ready PR=1110` passes, then squash-merge.
6. Update the Workbench issue and worklog, assemble and publish PR statistics, and verify the published handoff.

## Completion evidence

- Deterministic full-browser success for local browser-copy deletion and Sentinel participant response generation.
- The reported Devices & access identity-switch scenario and headless UI demo remain green.
- Exact-head Rust, WASM, web, extension, browser, deployment, policy, review, mergeability, and thread audits pass.
- PR 1110 is squash-merged and its Workbench issue, worklog, and statistics are visible on `main`.

## Safety review

This record contains no raw prompt, chat transcript, secrets, private data, raw logs, local paths, or unnecessary infrastructure detail.
