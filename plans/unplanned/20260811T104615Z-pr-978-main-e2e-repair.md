---
title: Repair PR 978 recovery-code browser regression
feature: unplanned
issue: issues/unplanned/nook-web-typescript-api-discipline.md
started_at: 2026-08-11T10:46:15Z
agent: codex
---

# Repair PR 978 recovery-code browser regression

## Interpreted request

Repair the merged-main extension E2E failure introduced by PR 978. Preserve
the browser regression's proof that malformed replacement cannot erase saved
recovery codes. Deliver the repair through an owned, validated, merged PR and
verify the resulting main head.

## Requirements

- Keep production secrets hidden from browser test APIs.
- Expose only non-secret recovery-code count metadata through Rust/WASM.
- Verify the saved code count remains unchanged after malformed replacement.
- Preserve immediate transport decoding and explicit invalid branches.
- Pass exact-head validation with zero unresolved review conversations.
- Squash-merge the repair and verify the resulting main pipeline.

## Constraints and exclusions

- Do not expose recovery-code values through list or transport responses.
- Do not modify foreign branches, PRs, issues, or features.
- Heavy product validation runs only on GitHub-hosted workers.

## Change budget and PR sequence

- Estimated authored changed lines: 120
- Owning modules, packages, or layers: nook-wasm authenticator metadata,
  extension session response mapping, and extension Playwright regression.
- Public or cross-module interfaces: non-secret authenticator account metadata.
- Delivery shape: One PR
- Current PR estimated authored changed lines: 120
- Current PR slice and acceptance evidence: Rust-owned backup-code count and browser preservation assertion; Acceptance evidence: exact-head validation and green merged main.
- PR slices and acceptance evidence: Rust-owned backup-code count and browser preservation assertion; Acceptance evidence: exact-head validation and green merged main.

## Initial plan

1. Add non-secret backup-code count to the Rust/WASM authenticator account.
2. Project that count through the extension session list response.
3. Replace the inaccessible Simple Vault assertion with before-and-after count
   assertions against the running extension session.
4. Format, validate, resolve current review feedback, and merge.
5. Publish completion records and verify the merged main head.

## Completion evidence

- Rust behavior tests cover backup-code count metadata.
- The extension Playwright test proves malformed replacement preserves count.
- Exact-head PR and merged-main pipelines pass.
- Workbench issue, worklog, and statistics are current.

## Safety review

This record contains no raw prompt, chat transcript, secret, private data, raw
log, local path, or unnecessary infrastructure detail.
