---
title: Route workflow shell into Taskfiles
feature: agent-workflow
issue: null
plan: plans/agent-workflow/20260805-163508-route-workflow-shell-to-taskfiles.md
nook_pr: 926
status: completed
started_at: 2026-08-05T16:35:08Z
finished_at: 2026-08-05T17:46:36Z
agent: cursor
---

# Work summary

## Outcome

Reusable GitHub workflow shell for Cloudflare deploy/domain verify, release metadata, dependency audit, Hive console, web-research, source-architecture, ci-agent smoke/plan, coverage staging, and Docker prune now lives in Taskfile-backed scripts. Workflows keep Actions-only glue as thin `task` wrappers. Squash-merged as PR 926.

## Progress

- Audited all workflow YAML files and classified shell as reusable vs Actions glue.
- Added `.task/ci-workflows.yml` and scripts under `.github/scripts/`.
- Thinned pr/main/release/hive/web-research/source-architecture/ci-agent-smoke/runner-cleanup/rust-dependency-updates/agent-implement workflows.
- Updated preflight delivery and Workbench contracts for the new ownership boundaries.
- Fixed CI failures around Task `ROOT_DIR`, SIGPIPE in surface checks, null/undefined smoke sentinels, oversized contracts, and planning-script contracts.

## Implementation problems

- Included Taskfiles needed `ROOT_DIR` for `dir:` instead of custom `REPO_ROOT`.
- `grep -q` under `pipefail` caused SIGPIPE (exit 141) in infra surface checks.
- Authored JS smoke script initially used forbidden `null`/`undefined` tokens.
- Contract tests still expected inline workflow shell after extraction.

## Decisions

- Large reusable shell moves into `.github/scripts/` with thin Task wrappers.
- Actions-only polling, attestation, and Workbench publishing stay in YAML.

## Validation

- Exact-head PR, Web research, Rust ecosystem, Hive, and Source architecture checks green on head `1cd949ee43d650d92f938de6601b52fc0babbae1`.
- `task pr:ready PR=926` reported ready before squash merge.

## Remaining work

- None.
