---
title: Repair protected identity switching before vault creation
feature: devices-and-access
issue: issues/devices-and-access/independent-local-identity-keyring.md
plan: plans/devices-and-access/20260826T001531Z-fix-protected-identity-switch.md
nook_pr: 1110
status: blocked
started_at: 2026-08-26T00:15:31Z
finished_at: 2026-08-26T01:33:25Z
agent: codex
---

# Repair protected identity switching before vault creation

## Outcome

PR 1110 repairs the blank screen after **Use identity** when two protected identities exist before any vault is created. The implementation and focused browser proof are complete, but merge is blocked by two pre-existing full-browser failures outside the Devices & access change surface.

## Progress

- Kept the route-owned Devices & access login dashboard mounted while the prior identity session is cleared.
- Added a reusable two-protected-identities-without-a-vault fixture.
- Added focused browser coverage for the pending protection state and successful identity activation.
- Added a headless UI demo of the reported flow.
- Aligned browser validation URL expectations with the PR-scoped URLs embedded in the verified preview artifact.
- Received clean advisory local and exact-head cloud Codex reviews with no unresolved conversations.

## Implementation problems

- Initial isolated browser jobs failed before Playwright output because of runner container and pod scheduling failures.
- Once product tests ran, the preview artifact correctly contained PR URLs while browser expectations still used production defaults. PR 1110 now passes the same PR URL variables to both build and browser validation.
- Three product-running full-browser cycles reproduced unrelated failures in `local-vault.spec.ts` browser-copy deletion and `device-key-protection.spec.ts` Sentinel participant-response generation. The requested Devices & access scenario passed in every product-running cycle.

## Decisions

- Preserve Rust/WASM identity activation and app-key protection ownership; the fix is limited to the web presentation lifecycle.
- Do not weaken or bypass the required full-browser gate.
- Do not expand PR 1110 into local-storage deletion or Sentinel onboarding without an explicit scope handoff.
- No Cortex update is required because the Devices & access product specification already says the dashboard must work before a vault exists.

## Validation

- `task loom:pre-push` passed on exact head `4280149a54170abb5be00e8de5aab4867cfd91f3`.
- Local Codex review found no actionable issue.
- Cloud Codex reviewed exact head `4280149a54170abb5be00e8de5aab4867cfd91f3` and found no major issue.
- Repository policy, Rust, WASM, web verification, extension E2E, UI demo, and preview deployment passed.
- All 14 Devices & access browser scenarios passed, including the new zero-vault protected identity switch.
- `task pr:ready PR=1110` reported only `PR run concluded failure`; exact-head deployment, mergeability, base freshness, and review-thread audits passed.

## Remaining work

- Repair the existing local browser-copy deletion and Sentinel participant-response full-browser failures in separately owned scope.
- Rerun full exact-head validation for PR 1110, pass readiness, squash-merge, then publish AI-agent statistics.
