# Relocate shared AI-debug tooling from `.codex`

## Outcome

Audited every file under `.codex` and moved the four live, shared AI-debug support assets to `.github/scripts/ai-debug/`. The `.codex` directory now contains only Codex-native configuration: `config.toml` and `hooks.json`.

## Progress

- Traced every script, policy module, manifest, and validator to its active callers in Task, Codex, Cursor, and Cortex documentation.
- Preserved all four assets because each has a live runtime or validation role.
- Updated Codex, Cursor, Task, and Cortex references to the central tooling paths.
- Preserved the isolated browser profile, loopback-only network policy, origin parity validation, cleanup behavior, and narrow Playwright MCP tool allowlist.
- Delivered and squash-merged [Nook PR #1010](https://github.com/meta-secret/nook/pull/1010).

## Implementation problems

No implementation defect or required PR-validation failure occurred. The directory-depth change required updating repository-root discovery in the launcher and origin checker. A separate post-merge `Main` run later stopped in BuildKit while loading the unchanged preflight Dockerfile; the complete exact-head PR run had already passed that repository code and all required jobs.

## Decisions

- Kept `.codex/config.toml` and `.codex/hooks.json` in place because they are Codex project configuration surfaces.
- Chose `.github/scripts/ai-debug/` for assets shared by Codex, Cursor, Task, and repository documentation.
- Did not delete any audited asset because all four were actively referenced and security-relevant.

## Validation

- `node .github/scripts/ai-debug/check-origins.mjs`
- `bash -n .github/scripts/ai-debug/run-playwright-mcp.sh`
- `task ai-debug:check`
- `task loom:pre-push`
- `task pr:review-local`
- Complete exact-head PR workflow run [31872182372](https://github.com/meta-secret/nook/actions/runs/31872182372)
- Exact-head Codex review: no major issues
- `task loom:pr-land` readiness audit: ready, current base, successful deployment, zero unresolved threads

## Remaining work

No follow-up is required for this refactor. The repository's existing Main failure handoff captured the post-merge BuildKit infrastructure signal separately.
