---
title: Work summary
feature: rust-action-ownership
issue: issues/rust-action-ownership/sync-projection-decisions.md
plan: plans/rust-action-ownership/2026-09-07T23-37-00Z-sync-projection-decisions.md
nook_pr: 1550
status: completed
started_at: 2026-09-07T23:37:00Z
finished_at: 2026-09-08T00:39:58Z
agent: codex
---

# Work summary

## Outcome

PR #1550 moved synchronization metadata, rollback-safe provider snapshot projection, vault replaceability assessment, and provider-vault decision projection onto owned Rust domain actions. The PR squash-merged at `6a89a2349bc1c6ed2ee2d006424fcc02a1b20f8a`.

## Progress

- `StorageProviderData` now owns sync checkpoint updates and `AuthProvidersSnapshotData` owns legacy wire projection.
- `CurrentVaultReplaceability` owns graph-based replaceability assessment and provider-vault decision projection; direct core, sentinel, storage, and WASM callers use the owned methods.
- Ownership denial and invalid-suppression prohibition are active in the completed sync modules, while the public WASM metadata adapter remains a thin boundary.
- Delivered 378 authored additions in one cohesive slice.

## Implementation problems

- The first hosted head attempted an inherent implementation for the event-log crate's external `EventGraph`; it was moved to the local `CurrentVaultReplaceability` owner.
- The next Dylint pass found unowned test fixtures and a duplicate associated fixture name; fixtures were placed under an owned test fixture type and the duplicate was renamed.
- Main advanced twice during validation; the branch was rebased onto the latest `main`, reformatted, size-checked, force-pushed, and revalidated.
- One remote Loom attempt hit an unrelated 5-second test timeout; the unchanged exact head was retried successfully.

## Decisions

- Preserved existing values, ordering, wire shape, redaction, conservative decision precedence, and public WASM signatures.
- Used existing domain owners and a small test fixture owner instead of introducing a generic phase framework.
- Kept the slice cohesive rather than splitting individual projections into micro PRs.

## Validation

- `cargo fmt` for core and WASM, `git diff --check`, and `task loom:pre-push` passed with 378 authored additions.
- Hosted validation run `34173153336` passed Native Rust, Dylint, WASM build and Node tests, web, preview, coverage, ecosystem, proof, dependency, fuzz, and policy checks.
- Remote Loom verification run `34173763907` passed.
- Exact-head deployment `https://pr-1550.nokey-sh.pages.dev` passed.
- `task pr:ready PR=1550` passed with current base, no unresolved feedback, and mergeability.
- PR #1550 squash-merged via GitHub API; `origin/main` was verified at `6a89a2349bc1c6ed2ee2d006424fcc02a1b20f8a`.

## Remaining work

- Continue the project-wide Rust action-ownership and typestate migration with the next bounded domain slice.
