---
title: Consuming Sentinel genesis issuance
feature: rust-action-ownership
issue: issues/rust-action-ownership/sentinel-genesis.md
plan: plans/rust-action-ownership/2026-09-05T03-13-05Z-sentinel-genesis-completion.md
nook_pr: 1363
status: completed
started_at: 2026-09-05T02:46:12Z
finished_at: 2026-09-05T04:42:42Z
agent: codex
---

# Sentinel genesis issuance delivery

## Outcome

[PR1363](https://github.com/meta-secret/nook/pull/1363) squash-merged as 73965ffc87e70af6997948766a4005ef6c5907aa. Private collecting and signer-bound ready owners consume through genesis issuance; rejection returns the sole collecting owner. Existing exact pending-output completion remains available through an explicit dashboard action.

## Ownership and behavior

The collecting session cannot be cloned, deserialized or directly constructed. Preparation borrows its checked signer and issuance consumes the ready owner. Actual Rust-future cancellation cannot restore issuance authority. Existing signature, version, label, duplicate and capacity validation retain their error precedence.

AwaitingCompletionCheck distinguishes a journal inspection requirement from confirmed persistence. Start discovers existing pending output and rejects new issuance. Finalize handles the exact saved output, preserves uncertainty on read errors and clears only the pending-check phase on confirmed absence. Both dashboards refresh actual Rust status after failures and expose the existing explicit completion action. No automatic restart or completion, new journal, recovery engine, cryptography or schema was introduced.

## Review and implementation corrections

Source review identified a terminal phase that would hide existing completion and a repeated-finalize path that could erase a completed delivery phase; both were corrected before publication. Hosted checks then identified test boundary types, must-use annotations, generated DTO disposal mocks, an obsolete mutable adapter, assertion style and explicit browser error conversions. Two complete repair batches preserved all checks and assertions. Removing the unused mutable ceremony accessor added one direct consumer-cleanup file to the plan's scope.

## Validation

Final head cf301651a94fa7687dde53b7536ccb32d95437ee passed full [run33944598463](https://github.com/meta-secret/nook/actions/runs/33944598463), source SECURITY and exact-head readiness with zero unresolved findings. The preceding authored-equivalent head passed all twelve new compile-fail probes and their positive control, all 157 browser tests including five genesis lifecycle cases, and all 659 web tests including seven dashboard cases. Required current-base gates passed again after generated-password integration. The retained invitation demo covers incomplete quorum; demo execution remains disabled and is not claimed as run.

The final diff contains 21 files, 1782 authored additions and three generated additions across two key artifacts. No local product compilation or tests ran. Rust domain tests remain inline, and every authored source file remains within the delivery limit.

## Limits and remaining mission

Local consuming ownership is not global replay prevention or an external authorization substitute. Existing journal clearing precedes final WASM conversion; no transactional or exactly-once delivery guarantee is claimed. Actual Rust-future drop was tested, not cancellation of a JavaScript Promise. The complete project migration continues through OAuth origin ownership, enrollment admission and the remaining domain inventory.

## Delivery assessment

Elapsed 6990 seconds over four validation cycles; two code-repair cycles and one later base refresh. No validation was cancelled. Compared with quorum PR1355's 13913 seconds and five cycles, this took 6923 fewer seconds; the comparison remains weak because implementation and runner conditions differ. A delayed dependency job succeeded without intervention. Existing typed-test and lint rules were reinforced in subsequent worker scopes; no new build-performance mechanism is inferred from this delivery history.
