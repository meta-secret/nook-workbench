---
title: Vault format action ownership
feature: rust-action-ownership
issue: issues/rust-action-ownership/vault-format-actions.md
plan: plans/rust-action-ownership/2026-09-07T16-06-28Z-vault-format-actions.md
nook_pr: 1529
status: completed
started_at: 2026-09-07T16:06:28Z
finished_at: 2026-09-07T17:25:28Z
agent: codex
---

# PR #1529 — Own vault format read and write actions

## Outcome

Migrated the vault format boundary to `VaultFormatDocument` and `VaultRecordSet`, moved YAML conversion and focused tests onto owning types, migrated direct core and WASM callers, and removed the old homeless format exports. Persisted YAML, browser ABI, normalization, ordering, and error behavior stayed unchanged.

## Progress

Rebased once onto the updated `origin/main` `33d6c41bb76638540783683651c4ae8ce5c2d430`, pushed exact head `0d91d78f8a6726472d8ac41668833a91ec8500b1`, passed hosted validation and remote Loom, then squash-merged PR #1529 as `5a7ff77e32d0dacb03f50fb29633ec85da3de5e7`.

## Implementation problems

Hosted policy required unit tests to be inline in the owning source module and rejected `anyhow` in `src`; the tests were converted to a local boxed-error result type. Hosted Rust/WASM checks also exposed an external unlock type, missing record borrows, one YAML semicolon, fixture indentation, and three browser-test callers still using removed free functions. Each was corrected before the final exact-head run.

## Decisions

Kept the scope to vault format ownership and its direct callers. `VaultUnlock` remains owned by `nook-auth2`, so unlock normalization is a private `VaultRecordSet` action rather than an orphan impl. No compatibility free functions, schema changes, cryptographic changes, storage changes, or blanket lint suppressions were added.

## Validation

- `cargo fmt --manifest-path nook-app/nook-platform/Cargo.toml --all -- --check` passed after rebase.
- `git diff --check` passed.
- `task loom:pre-push` passed with 1,019 authored additions and base `33d6c41bb76638540783683651c4ae8ce5c2d430`.
- Hosted repository policy run `34146837265` passed.
- Hosted PR/product run `34146845580` passed: native Rust, WASM build and Node tests, web verification, deployment preview, and required checks.
- Remote `loom:verify` run `34147413310` passed.
- `task pr:ready PR=1529` reported `ready: true` with no reasons.
- PR merge and `origin/main` verification passed at `5a7ff77e32d0dacb03f50fb29633ec85da3de5e7`.

## Remaining work

Continue the next cohesive Rust action-ownership slice from the dependency chain, beginning with the next open issue under `rust-action-ownership`.
