---
title: Device access profile and grant action ownership
feature: rust-action-ownership
issue: issues/rust-action-ownership/device-access-actions.md
plan: plans/rust-action-ownership/2026-09-07T18-20-00Z-device-access-actions.md
nook_pr: 1538
status: completed
started_at: 2026-09-07T18:20:00Z
finished_at: 2026-09-07T20:10:15Z
agent: codex
gizmo_id: rust-action-ownership-device-access-actions
---

## Outcome

PR #1538 moved nine device-access operations and adjacent passkey classification onto owned Rust action types, migrated core/WASM/storage callers, and removed the homeless exports. The main-branch WASM composition-test module was also routed through the existing internal API expert so hosted Loom policy remained green. The PR squash-merged at `3348c3746f8b3799a72ec5c0f47fcdb4dd3e59cf`.

## Progress

- Added data-carrying owners for identity links and grants, profile decoding, protection and identity observations, safe credential identifiers, and label/name normalization.
- Preserved profile wire shape, version behavior, errors, safe hash prefixes, and public WASM behavior.
- Added the new composition-test crate to the Cortex/Loom module-expert registry and updated its catalog count.
- Final exact head was `007c9f14c6e7b67313a0aecb1a649f4ec9e79650` on base `971e77651009618b4f295ff596b80eb1f839025a`; authored additions were 422.

## Implementation problems

- The first hosted cycle exposed five action-module lint defects; they were fixed without behavior changes.
- Main advanced repeatedly during delivery. Each advance was rebased, formatted, and revalidated.
- Remote Loom initially exposed the newly merged composition-test module as unrouted; the Cortex/catalog entry and count were updated, after which the exact-head remote run passed.

## Decisions

- Keep browser ceremony and persistence orchestration in adapters while portable classification and normalization stay on domain owners.
- Reuse `internal_api_expert` for the WASM composition-test crate instead of adding a second bridge role.
- Keep the action migration and required main-branch policy repair in one cohesive PR.

## Validation

- `cargo fmt --manifest-path nook-app/nook-platform/Cargo.toml --all -- --check`, `git diff --check`, and `task loom:pre-push` passed on the final rebased head.
- `task loom:module-experts:validate` and the focused Loom audit test passed; `task preflight:loom-contracts` passed.
- Hosted repository policy `34157817329` and PR validation `34157831697` passed, including required Rust, WASM, Node, web, coverage, and exact-head Pages deployment.
- Remote `loom:verify` `34157843785` passed.
- `task pr:ready PR=1538` passed, then exact-SHA squash merge completed and `origin/main` advanced to `3348c3746f8b3799a72ec5c0f47fcdb4dd3e59cf`.

## Remaining work

Continue with the next cohesive action-ownership slice from the refreshed main branch.
