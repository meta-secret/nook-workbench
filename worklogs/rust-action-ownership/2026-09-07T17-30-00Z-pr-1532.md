---
title: Vault metadata projection and event-graph access action ownership
feature: rust-action-ownership
issue: issues/rust-action-ownership/vault-meta-projection-actions.md
plan: plans/rust-action-ownership/2026-09-07T17-30-00Z-vault-meta-projection-actions.md
nook_pr: 1532
status: completed
started_at: 2026-09-07T17:30:00Z
finished_at: 2026-09-07T18:18:45Z
agent: codex
gizmo_id: rust-action-ownership-vault-meta-projection-actions
---

# PR #1532 — Vault metadata projection and event-graph access action ownership

## Outcome

Six homeless vault metadata and event-graph adapter operations now belong to small data-carrying owners. PR #1532 was squash-merged at `456f149837aa32854835eaa68a3010020be65ddc` from exact head `5181266c84db7186aeb054d9144a6d321531ac79` onto base `735b5e5fba425576035543c858df06c7b0eab518`.

## Progress

- `VaultMetaOperationApplier` owns metadata replay and consuming replacement.
- `VaultMetaGraphProjection` owns graph materialization while preserving event ordering, checkpoint replacement, and error behavior.
- `EventGraphDeviceAccess` and `EventGraphAuthorizationProjection` own identity-bound active-device and authorization-recipient queries.
- `SentinelMemberRecordProjection` owns encrypted member-row reconstruction.
- Core, WASM, storage, workflow, and colocated tests use typed owners; the six old free exports are absent.
- The slice added 651 authored lines and kept changed source files below the repository size limit.

## Implementation problems

- Initial hosted validation exposed a timestamp view mismatch, a missing fallible `?`, and stale imports; the focused fix preserved behavior.
- Native Clippy then required borrowed request inputs and `#[must_use]` constructors; the cleanup was committed and revalidated.
- The first successful hosted cycle became obsolete when main advanced; exact-head validation, security review, remote Loom, and readiness were rerun after rebase.

## Decisions

- Keep the migration bounded to projection and access evidence owners; recipient encryption and identity-genesis construction remain separate actions.
- Preserve the existing core, WASM, storage, workflow, and browser-facing contracts while removing only the six migrated free operations.
- Rebase and repeat all exact-head delivery gates whenever main advances.

## Validation

- Scoped `cargo fmt --manifest-path nook-app/nook-platform/Cargo.toml --all -- --check`, `git diff --check`, and `task loom:pre-push` passed after the final-main rebase. No local product builds or tests were run.
- Exact-head SECURITY review passed with no actionable P1/P2/P3 findings.
- Hosted repository policy run `34150262155` and hosted PR run `34150277057` passed, including native Rust, WASM build and Node tests, Dylint, web verification, coverage, and the exact-head Pages deployment.
- Remote `loom:verify` run `34150999225` passed.
- `task pr:ready PR=1532` passed; mergeability, unresolved threads, required jobs, and deployment were verified before the exact-SHA squash merge.
- Main advanced during delivery; the branch was rebased onto `735b5e5fba425576035543c858df06c7b0eab518` before the final validation cycle.

## Remaining work

Continue with the next cohesive action-ownership slice from the refreshed `origin/main`; recipient encryption, identity-genesis construction, and broader auth2 primitive migration remain outside this PR.
