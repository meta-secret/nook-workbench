# Worklog: PR 925

Linked plans:
- `plans/agent-workflow/20260805-161000-web-bake-retry-isolation-flake.md`
- `plans/agent-workflow/20260805-161743-ecosystem-taskfile-cache-from.md`

## Done
- Web Bake retries only for known BuildKit frontend/Dockerfile-load flakes.
- Companion WASM readiness wait before `createManifest` in verify-app-isolation.
- Rust ecosystem CI now calls `task docker:ecosystem:*`; removed Bake helper script.
- Isolated PR/Remote BuildKit `cache-from` refs use `ignore-error=true` so cold
  `nook/remote-buildcache/**` imports fall back to Main.

## Validation
- Hosted `task remote TASK_NAME=preflight` passed on `ae948a669`.
- Exact-head `task pr:validate` green twice (Taskfile/cache fix head, then after
  merging `origin/main`).
- Squash-merged as `b15aa73e4`.
