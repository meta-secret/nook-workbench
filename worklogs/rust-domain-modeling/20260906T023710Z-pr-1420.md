---
title: Typed Nook Core facade activated
feature: rust-domain-modeling
issue: null
plan: plans/rust-domain-modeling/20260906T002000Z-nook-core-facade-activation.md
nook_pr: https://github.com/meta-secret/nook/pull/1420
status: completed
started_at: 2026-09-06T00:20:00Z
finished_at: 2026-09-06T02:37:10Z
agent: codex
---

# Typed Nook Core facade activation

## Outcome

PR 1420 was squash-merged as `5ae026cf9a80dfa3262c2b44df58c21c39ebab38`, with 900 authored additions across 52 paths, excluding lockfiles.

## Progress

- Eliminated all 51 inherited raw-numeric facade diagnostics that blocked Nook Core activation.
- Added private-storage WebAuthn, canonical-event, and event-storage byte domain types.
- Typed the Auth2 WebAuthn and Event Log facade surfaces while preserving stable Core and WASM consumers.
- Kept serialization, database, cryptographic, WebAuthn, YAML, and IndexedDB conversions at narrow boundary adapters.
- Activated `nook-core` with forbidden invalid suppression and denied raw numeric public APIs.

## Implementation problems

- Hosted compilation found missing imports, test fixture conversions, moved event-storage bytes, and one WASM implicit clone; each was corrected without weakening the lint.
- Main advanced twice. The larger integration added checked remote-event writes and outbox lifecycle changes; those contracts were preserved while threading typed event bytes through the new architecture.
- The first post-integration run found one duplicate inherited `sha256_hex` import. Removing it retained the local typed Core adapter.

## Decisions

- Kept the full typed facade activation in one cohesive 900-addition PR, below the user-set 1,500-addition ceiling.
- Did not add use-item, blanket, or inherited-facade suppression.
- Preserved Main's checked-event, prepared-union, visibility, and publish/acknowledge lifecycle semantics.

## Validation

- Focused Rustfmt, diff, conflict-marker, raw-byte surface, suppression, consumer, and file-size checks passed.
- `task loom:pre-push` passed at 900 authored additions after final Main integration.
- [Final exact-head hosted validation](https://github.com/meta-secret/nook/actions/runs/34006493337) passed at `dce1e0e9d3c9ec0f0698da22e3a1dca428a6edb8` on Main `4b41d2a842282f509c962ad5aba180ab5182f05c`.
- Native Rust and coverage, WASM build and Node tests, web verification, Dylint, dependency policy and RustSec, fuzz smoke, Kani, Proptest/Insta/Loom, repository policy, and preview deployment passed.
- Exact-head readiness passed before squash merge.

## Remaining work

- Inventory the remaining Rust crates against the active domain-API lint.
- Migrate the next dependency-safe crate cohort, keeping each PR cohesive and below 1,500 authored additions.
