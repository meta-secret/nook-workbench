---
title: Preserve local vault and honest unlock after import-as-new-vault
feature: vault-event-log
issue: issues/backlog/gh-205-guard-provider-connect-against-silent-vault-replacement.md
plan: plans/vault-event-log/20260801-023527-preserve-import-vault-unlock.md
nook_pr: https://github.com/meta-secret/nook/pull/898
status: completed
started_at: 2026-08-01T02:40:00Z
finished_at: 2026-08-01T03:40:15Z
agent: cursor-grok
---

# Preserve local vault and honest unlock after import-as-new-vault

## Outcome

Squash-merged [Nook PR #898](https://github.com/meta-secret/nook/pull/898). Choosing **Import as new vault** on a `store_id` mismatch keeps the prior empty local vault on this device, shows both vaults in the login picker, hydrates backup-password entries for locked imports, and disables device-key unlock when this device has no auth envelope.

## Progress

- Hydrated locked import projections from the event graph (password entries + meta) before persist.
- Preserved the prior vault blob/registry after provider import-as-local.
- Cleared forced login selection when more than one local vault remains after import.
- Surfaced `loginDeviceKeysCapable` so the auth form offers backup password instead of a raw missing-envelope failure.
- Added WASM browser regression, Playwright conflict coverage, and a UI demo for unlock honesty.
- Fixed browser-test compile blockers (`VaultApplication::Simple`, auth-provider Option handling) so the suite builds again.

## Implementation problems

- Focused `wasm:test:browser` remote runs still hit an intermittent ChromeDriver SIGKILL/404 after the suite compiled; merge authorization used exact-head PR validation instead.
- Untracked local `fuzz/target/` briefly dirtied the worktree before remote dispatch.

## Remaining work

- None for this import/unlock honesty gap.
- Broader provider-connect metadata/UX from the parent backlog item remains historical context only; this PR closed the concrete scenario-B failure path.
