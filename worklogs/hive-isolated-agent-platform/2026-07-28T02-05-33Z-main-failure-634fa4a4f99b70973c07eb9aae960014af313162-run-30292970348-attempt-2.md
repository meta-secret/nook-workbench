---
title: Main repair blocked by active shared regression PR
feature: hive-isolated-agent-platform
issue: issues/hive-isolated-agent-platform/main-failure-634fa4a4f99b70973c07eb9aae960014af313162.md
plan: plans/hive-isolated-agent-platform/2026-07-28T02-04-55Z-main-failure-634fa4a4f99b70973c07eb9aae960014af313162-run-30292970348-attempt-2.md
nook_pr: null
status: blocked
started_at: 2026-07-28T02:04:55Z
finished_at: 2026-07-28T02:05:33Z
agent: codex
---

# Main repair blocked by active shared regression PR

## Outcome

Diagnosis completed, but delivery is blocked to avoid a competing repair. Pull
request #823 already owns the same stale bootstrap mount regression and has
not yet completed its required validation.

## Progress

- Inspected the failed Main run, its retained UI-demo and browser artifacts,
  the incident state, later Main runs, and open repair pull requests.
- The failure pattern is consistent with the application bootstrap attempting
  to mount after a content or demo document has been replaced during WASM
  initialization. This cascades into browser and extension test timeouts.
- Published the task-start plan and recorded the active shared repair as the
  blocker.

## Implementation problems

- The deterministic branch for this incident has no PR, but #823 is an active
  Main-repair PR for the same bootstrap failure. Its current head is still red,
  so it cannot yet be adopted as a completed fix.

## Decisions

- Do not open a duplicate PR or independently modify the same bootstrap path.
  Resume only after #823 is merged or closed; it must first satisfy its
  exact-head checks and Main-equivalent browser suites.

## Validation

- Failed Main run: 30292970348 (UI demos, Web e2e, Extension e2e).
- Active shared repair: Nook PR #823, labeled `ci:full-e2e`; its current
  repository checks include failures and an in-progress extension suite.
- No product files were changed and no local product validation was run.

## Remaining work

- Wait for PR #823 to reach a terminal state. If it merges, verify the
  resulting Main workflow before closing this incident; if it closes or fails,
  resume this incident on its deterministic repair branch.
