---
title: Main Access e2e repair completion
feature: hive-isolated-agent-platform
issue: issues/hive-isolated-agent-platform/main-failure-2ca5a371e2d616a8beaa8360cb4712d6c6279299.md
plan: plans/hive-isolated-agent-platform/2026-08-20T034804Z-main-failure-2ca5a371e2d6-run-32327997364-attempt-1.md
nook_pr: 1060
status: completed
started_at: 2026-08-20T03:44:35Z
finished_at: 2026-08-20T04:27:06Z
agent: codex
---

# Main Access e2e repair completion

## Outcome

PR 1060 restored Main verification by updating the Access browser regression
to the intended current layout. The pull request passed exact-head full browser
and extension suites, squash-merged, and its resulting Main workflow completed
successfully.

## Progress

- Correlated the retained failure evidence with the Access layout change that
  removed the duplicate current-identity browse row.
- Replaced the obsolete button locator with assertions for the selected
  Identity perspective and its exact one-vault headline.
- Published the deterministic Hive branch and PR with the `hive` and
  `ci:full-e2e` labels.
- Audited existing feedback, completed exact-head readiness, squash-merged, and
  monitored the resulting Main run through terminal success.

## Implementation problems

- The GitHub CLI label editor referenced a retired Projects-classic field. The
  labels API supplied the same repository event without changing workflow
  policy.
- Adding both required labels in one API request emitted separate label events;
  the later non-validation label cancelled the first run through workflow
  concurrency. Reapplying only the validation label produced the clean
  authoritative exact-head run.
- Local and cloud review requests were unavailable in the worker environment.
  The review audit found no submitted reviews, substantive comments, or inline
  threads, so repository guidance allowed delivery after green checks.

## Decisions

- Kept the repair test-only because the persisted application milestones showed
  normal vault setup and the removed row was an intentional product decision.
- Asserted both selected perspective state and the replacement headline so the
  regression covers behavior rather than merely changing to another locator.
- Preserved the initial cancelled and unsupported-label runs as workflow
  evidence and used the later full-e2e run as the readiness authority.

## Validation

- `task loom:pre-push` passed before the owner push.
- PR workflow 32329725872 passed every repository-owned job, including full
  browser e2e and full extension e2e, for the exact PR head.
- `task hive:guest:pr:ready PR=1060` returned `ready: true`.
- Main workflow 32330536287 completed successfully for squash commit
  `271e05fc658b2b058e03e8e1d06a9ccb2c8392d0`.

## Remaining work

None.
