---
title: Main repair resume blocked for 401cd489392e
feature: hive-isolated-agent-platform
issue: issues/hive-isolated-agent-platform/main-failure-401cd489392e65e63718058677301ad36f358fac.md
plan: plans/hive-isolated-agent-platform/2026-07-28T11-15-00Z-main-failure-401cd489392e-run-30349125582-attempt-1.md
nook_pr: null
status: blocked
started_at: 2026-07-28T12:22:00Z
finished_at: 2026-07-28T12:30:00Z
agent: codex
---

# Main repair resume blocked for 401cd489392e

## Outcome

The Main repair remains blocked before code publication because this resumed
Hive guest cannot run the mandatory formatter.

## Progress

- Inspected the existing incident, previous attempt, deterministic repair
  branch state, current Main, retained workflow jobs, and browser-suite logs.
- Confirmed the shared browser failure: a secret-page query can run during
  reload/background synchronization before cached vault crypto is restored.
- Confirmed no existing repair PR or deterministic repair branch is present.

## Implementation problems

- `task hive:guest:format` invokes `cargo fmt --all`, but `cargo` is absent
  from this guest's executable path. The task fails with exit status 127.

## Decisions

- Do not bypass the mandatory formatter, manufacture a formatting result, or
  push an unformatted branch. Resume in a correctly provisioned Hive guest.

## Validation

- GitHub Actions Main run 30349125582: Web e2e and Extension e2e failed; the
  retained browser evidence identifies the vault-crypto initialization error.
- `task hive:guest:format`: blocked because Cargo is unavailable.

## Remaining work

- Provision Cargo/Rust formatting support in the Hive guest, then implement
  the scoped WASM repair and browser regression; publish, validate, squash
  merge, verify the replacement Main run, and publish PR statistics.
